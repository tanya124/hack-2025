# Инструкции по расширению функционала бота для изучения межславянского языка

## 1. Создание и генерация учебного плана

### Структура базы данных
1. Создать таблицу `study_plans` в базе данных:
```sql
CREATE TABLE IF NOT EXISTS study_plans (
    id SERIAL PRIMARY KEY,
    user_id BIGINT REFERENCES users(user_id),
    level VARCHAR(50),
    goal VARCHAR(100),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

2. Создать таблицу `study_plan_items` для хранения элементов учебного плана:
```sql
CREATE TABLE IF NOT EXISTS study_plan_items (
    id SERIAL PRIMARY KEY,
    study_plan_id INTEGER REFERENCES study_plans(id),
    topic VARCHAR(255),
    description TEXT,
    order_number INTEGER,
    bloom_level INTEGER,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

3. Создать таблицу `progress` для отслеживания прогресса:
```sql
CREATE TABLE IF NOT EXISTS progress (
    id SERIAL PRIMARY KEY,
    user_id BIGINT REFERENCES users(user_id),
    study_plan_item_id INTEGER REFERENCES study_plan_items(id),
    is_completed BOOLEAN DEFAULT FALSE,
    current_bloom_level INTEGER DEFAULT 1,
    correct_answers INTEGER DEFAULT 0,
    total_attempts INTEGER DEFAULT 0,
    last_attempt_at TIMESTAMP,
    completed_at TIMESTAMP
);
```

### Генерация учебного плана
Создать функцию в `openai_service.py` для генерации учебного плана на основе уровня пользователя и цели обучения, используя учебник "Interslavic zonal contructed language: An introduction".

## 2. Отслеживание прогресса

Добавить методы в `database.py` для:
- Сохранения учебного плана
- Получения учебного плана пользователя
- Обновления прогресса
- Получения текущей темы

## 3. Генерация заданий на основе учебного плана

Обновить функцию генерации заданий в `openai_service.py` для создания микро-уроков и вопросов на основе темы и уровня таксономии Блума.

## 4. Обновление обработчиков в боте

Добавить новые обработчики в `enhanced_bot.py` для:
- Показа учебного плана
- Перехода к следующей/предыдущей теме
- Получения заданий по текущей теме
- Обработки ответов пользователя

## 5. Обновление меню и обработчика callback-запросов

Добавить новые пункты меню и обработчики для callback-запросов от кнопок.

## 6. Типы микролернинг-заданий по таксономии Блума

### 🔹 1. Запомнить (Remember)
- Тип: «Слово дня»
- Бот даёт слово, перевод, произношение и контекст
- Вопрос: "Что значит слово *вѣра*?"
- Цель: закрепить базовый словарь

### 🔹 2. Понять (Understand)
- Тип: «Найди смысл»
- Даётся короткая фраза на межславянском с современным переводом
- Вопрос: «Какой из этих переводов точнее передаёт смысл?»
- Цель: развивать интуицию языка

### 🔹 3. Применить (Apply)
- Тип: «Собери фразу»
- Даётся современное предложение
- Варианты слов в межславянском (в перемешку)
- Пользователь должен составить правильную фразу
- Цель: использовать слова и грамматику

### 🔹 4. Проанализировать (Analyze)
- Тип: «Что здесь не так?»
- Бот показывает предложение с грамматической/лексической ошибкой
- Вопрос: «Найди ошибку» или «Исправь неверное слово»
- Цель: развить языковую чуткость

### 🔹 5. Оценить (Evaluate)
- Тип: «Сравни переводы»
- Даются два варианта перевода одной фразы
- Вопрос: «Какой перевод ближе к духу оригинала и почему?»
- Цель: научиться интерпретировать и оценивать стили

### 🔹 6. Создать (Create)
- Тип: «Сочини своё»
- Бот даёт шаблон фразы (например: «Да будет ___ во мне»)
- Пользователь вводит слово или выбирает из предложенных
- Цель: выразить мысль в стиле древнего языка

## 7. Дополнительные требования

- При генерации задач и теории нужно описаться на учебник по межсловянскому языку (Merunka_V_Interslavic_zonal_contructed_language_An_introduction.pdf)
- Задания должны соответствовать текущему уровню пользователя по таксономии Блума

### 🧠 Prompt for OpenAI API (RU-Lang Output)

```text
Ты — преподаватель межславянского языка. Твоя задача — генерировать короткие обучающие задания в стиле микролёрнинга, подходящие для использования в Telegram-боте.

Каждое задание должно включать:

1. Краткое теоретическое объяснение (максимум 3–5 предложений) на русском языке. Объяснение должно касаться одного понятия: слово, корень, буква, грамматическая форма.
2. Один вопрос в формате викторины с 2–4 вариантами ответа на русском. Один из них должен быть правильным, остальные — правдоподобные, но ошибочные.
3. Укажи правильный вариант ответа для автоматической проверки.

Выводи результат в формате JSON:

{
  "lesson": "<объяснение>",
  "question": "<вопрос>",
  "options": ["А", "Б", "В", "Г"],
  "correct_answer": "Б"
}

Контент должен быть рассчитан на начинающих. Используй межславянские слова, корни, алфавит, церковные и исторические примеры. Объяснение и задание — **на русском языке**, но со вставками межславянских слов и форм.
```
